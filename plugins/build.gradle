subprojects {
    apply {
        plugin('java-gradle-plugin')
    }

    dependencies {
        implementation localGroovy()
        implementation gradleApi()
    }

    test {
        useJUnitPlatform()
    }
}