package org.toolkit4j.libs.io.impl;

import org.junit.jupiter.api.Test;

class LocalFSTest {

	@Test
	void touch() {
		// var p = new LocalFS().touch(System.getProperty("user.dir") + "test");
		// System.err.println(p.getAbsolutePath());
	}

	@Test
	void mkdir() {
		// val dir = new LocalFS().mkdir(System.getProperty("user.dir") + "test",
		// false);
		// System.err.println(dir.internal().getAbsolutePath());
	}
}
