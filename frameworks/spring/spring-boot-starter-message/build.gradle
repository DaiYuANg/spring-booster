group = 'org.kop.framework.spring.starter.message'
version = '1.0-SNAPSHOT'

dependencies {
//    implementation(project(":frameworks:spring:spring-boot-starter-kernel"))
}

test {
    useJUnitPlatform()
}