plugins {
    id "com.github.node-gradle.node" version "${nodePluginVersion}"
}

ext {
    dockerSDKVersion = "3.3.1"
    openapiVersion = "2.0.2"
    oshiVersion = "6.4.4"
    mysqlVersion = "8.0.33"
}

dependencies {
    implementation("com.github.docker-java:docker-java-transport-httpclient5:${dockerSDKVersion}")
    implementation("com.github.docker-java:docker-java-core:${dockerSDKVersion}")
    implementation("org.springframework.boot:spring-boot-starter-thymeleaf")
    implementation("org.springframework.boot:spring-boot-starter-websocket")
    implementation(project(":frameworks:spring:spring-boot-starter-async"))
    implementation(project(":standard:restful"))
    implementation(project(":frameworks:spring:spring-boot-starter-scheduled"))
    compileOnly("com.mysql:mysql-connector-j:${mysqlVersion}")
    implementation("org.springdoc:springdoc-openapi-starter-webmvc-ui:${openapiVersion}")
    implementation("com.github.oshi:oshi-core:${oshiVersion}")
    implementation("org.buildobjects:jproc:2.8.2")
}

node {
    download = true
    distBaseUrl = "https://npmmirror.com/mirrors/node/"
    version = "18.16.1"
    nodeProjectDir = file("./ui")
}

tasks.register('devUI', YarnTask) {
    args = ['dev']
}

tasks.register('buildUI', YarnTask) {
    args = ['build']
}

tasks.register('copyDist',YarnTask){

}